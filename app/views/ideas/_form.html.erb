<%= form_for [current_user, @idea] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %> &nbsp

  <%= f.submit class: "btn btn-success btn-sm", method: :post %>

<% end %><br>
