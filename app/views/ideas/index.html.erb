<% current_user.ideas.each do |idea| %>
  <%= link_to idea.name, user_idea_path(current_user, idea.id) %>
  <br><br>
<% end %>
<br>

  <%= button_to "Create Idea", new_user_idea_path(current_user), class: "btn btn-primary btn-sm", method: :get%>
